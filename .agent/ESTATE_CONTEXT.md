# Mickey Estate Context

Mickey exists in the Genesis-managed estate as the first local OTA Tool Gateway / MCP proof workspace.

Canonical repo:

- `https://github.com/calvinchanht/ota-computer-use-gateway`

Current deployment target:

- Mickey public API endpoint is served through Cloudflare Tunnel for ChatGPT Business connector testing.
- The bearer token remains server-side/secret and must never be pasted into chat or logs.

Roadmap relationship:

- Mickey proves the OpenClaw-like chat-thread agent model.
- Catalyst is the next target once Mickey bootstrap/context/browser capability is proven.
- Boba/Mac CUADriver work is intentionally after Catalyst browser-workspace anchoring.

Safety reminders:

- Public tunnel mode must not allow loopback auth bypass.
- CAPTCHA / Turnstile / human verification is Calvin-only.
- External irreversible actions require Calvin.
