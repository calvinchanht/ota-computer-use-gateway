# Mickey User Context

Primary human: Calvin.

Calvin's current goal for this workspace:

- make provider chat-thread agents as capable as OpenClaw-style agents through OTA Tool Gateway;
- prove the pattern with Mickey first;
- then anchor Catalyst as a ChatGPT.com / ChatGPT app thread for browser-based job-application workflows;
- defer deeper Boba/Mac CUADriver work until after Catalyst browser-workspace anchoring is practical.

Preferences:

- Capability first; do not handicap chat-thread agents into a tiny toy tool surface.
- Prefer provider-neutral primitives and scoped proxy layers.
- Add intervention, approval, and extra guardrails as layers after base capability is usable.
- Keep secrets private and never paste bearer tokens or PATs.
