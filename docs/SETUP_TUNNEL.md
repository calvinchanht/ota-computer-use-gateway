# Secure MCP Tunnel Setup

This project is designed for OpenAI Secure MCP Tunnel. The local MCP server should bind to `127.0.0.1` and be connected through the official tunnel client.

Exact ChatGPT connector setup steps will be filled in after the first local server smoke test.
